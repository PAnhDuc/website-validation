name: Kiểm tra dự án

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Kiểm tra mã nguồn
        uses: actions/checkout@v3

      - name: Thiết lập Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 16

      - name: Cài đặt các gói phụ thuộc
        run: npm install

      - name: Kiểm tra cấu trúc dự án
        run: |
          echo "🔍 Đang kiểm tra cấu trúc dự án..."
          node check-structure.js || exit 1

      - name: Chạy ESLint (Kiểm tra coding convention)
        run: |
          echo "🔍 Đang kiểm tra coding convention bằng ESLint..."
          npm run lint > eslint-report.txt || exit 1
          echo "✅ Đã hoàn thành kiểm tra coding convention."
          if grep -q 'error' eslint-report.txt; then
            echo "⚠️ Phát hiện lỗi trong coding convention!"
            cat eslint-report.txt
          else
            echo "🎉 Không có lỗi trong coding convention."
          fi

      - name: Chạy Jest tests (Kiểm tra logic code)
        run: |
          echo "🔍 Đang kiểm tra logic code với các bài kiểm tra..."
          npm test > jest-report.txt || exit 1
          echo "✅ Đã hoàn thành kiểm tra logic code."
          if grep -q 'FAIL' jest-report.txt; then
            echo "⚠️ Phát hiện lỗi logic code!"
            cat jest-report.txt
          else
            echo "🎉 Tất cả các bài kiểm tra logic code đã vượt qua."
          fi

      - name: Hoàn thành công việc
        run: echo "Tất cả các bước kiểm tra đã hoàn thành."
